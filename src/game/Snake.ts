import GameObject from "../common/GameObject"

export default class Snake extends GameObject {
  constructor(private speed = 10) {
    super()
    this.init()
  }

  init() {
    
  }

  render(dt: number): void {
    
  }
}